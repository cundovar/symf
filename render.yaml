services:
  - type: web
    name: symfony-app
    env: docker
    plan: free
    envVars:
      - key: APP_ENV
        value: prod
      - key: APP_SECRET
        generateValue: true
      - key: DATABASE_URL
        value: "pgsql://user:password@host:5432/dbname"
