{{ form_start(form) }}
    {{ form_widget(form, {

        attr:{"class": ' text-black bg-red-500 '},
    }) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
